---
description: Shell 父层与 iframe 子层 — 路由闭环、懒加载、postMessage、样式隔离
globs: index.html,page-*.html,page-map-r3.js,page-map-r3.css
alwaysApply: false
---

# Shell 与页面路由

死守目标：**Shell（父层）与 iframe（子层）通信管道不崩塌**。

## 路由闭环红线

新页面必须**双向注册**，缺一不可：

1. `index.html` → `PAGES` 表：`key`、`src`、`nav`、`label`、`bc`
2. `index.html` → `<iframe class="pframe" id="f-{key}" data-src="...">` 列表（`home` 可直写 `src`，其余用 `data-src` + 空 `src`）

需要导航入口时同步 `.nl` 元素；V1 隐藏项保持 `display:none`。

## 懒加载时序

- 除 `home` 外：**切换时才注入 `src`**（`go(key)` 首次加载 `assetUrl(PAGES[key].src)`）
- 新 key 必须加入 `loaded` Set 逻辑，勿破坏 `SITE_BUILD` 缓存刷新机制

## 跨页通信（死锁消息类型）

子页 → 父页仅用 `window.parent.postMessage`，类型不得自造：

```javascript
// 基础跳转
{ type: 'navigate', page: 'offline-festival' }

// 锚点跳转 — section 对齐子页 id="sec-xxx"
{
  type: 'navigateAndScroll',
  payload: { page: 'offline-festival', section: 'sec-films' }
}

// 提示 / 登录同步（已有）
{ type: 'toast', payload: { msg: '...' } }
{ type: 'login', payload: { profile } }
{ type: 'logout' }
```

父页 → 子页滚动：`{ type: 'scrollTo', section: 'sec-xxx' }`。

**上影节 section 对照**（见 `CONTEXT.md`）：`sec-banner`、`sec-lists`、`sec-films`、`sec-checkin`、`sec-discuss`。

## 懒加载 + 消息防丢失

目标 iframe **尚未加载**（`src` 为空 / 未 `loaded`）时：

- Shell **必须缓存**待执行的 `navigate` / `navigateAndScroll` / `scrollTo` 指令
- 子页初始化完成后向父页发送 `{ type: 'ready', page: 'xxx' }`
- Shell 收到 `ready` 后**补发**缓存指令，再清空队列

修改 `navigateAndScroll` 时禁止仅靠固定 `setTimeout` 碰运气；优先走 ready + 队列机制。

## 样式防污染隔离

- 子页根容器使用独立前缀类（如 `.ph-root`、`.siff-root`），**所有**选择器挂在前缀下
- 禁止污染 `index.html` 全局或兄弟 iframe
- 移动端兜底：`min-height: 100dvh`、长文本 `word-break` / `overflow-wrap`、可点区域 ≥ 44px、横向不溢出

## 地图

- Hash：`#map?city=xxx`；`slugToMapCityId` 映射后调 iframe 内 `selectCity(id)`
- 复杂逻辑：`page-map-r3.js` + `page-map-r3.css`
