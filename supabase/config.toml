# Cine Ours — Supabase 本地 CLI 配置
# 部署: npx supabase functions deploy proxy-image --project-ref dirrnojiybbwotuqeqww

project_id = "dirrnojiybbwotuqeqww"

[functions.proxy-image]
# 油猴 publishable key 非 JWT；关闭校验后脚本可走中转上传
verify_jwt = false
